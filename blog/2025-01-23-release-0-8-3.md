---
slug: release-0-8-3
title: "0.8.3: Cleaner Loads"
authors: [overlock]
tags: [release, bugfix]
---

Small image-loader refinement and a cleaner failure mode when no kube context exists.

<!-- truncate -->

## What's new

- **`LoadPackageDirectory` accepts kinds** for filtering — load only what you need from a package directory.

## Bug fixes

- **No more global error when there's no kube context** — the CLI fails specifically where it needs a context, not before.
