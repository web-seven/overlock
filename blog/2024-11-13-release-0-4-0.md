---
slug: release-0-4-0
title: "0.4.0: Engine and Namespace, Spelled Out"
authors: [overlock]
tags: [release, registry]
---

Registry creation gains explicit `--engine-name` and `--engine-namespace` parameters so multi-engine setups are unambiguous.

<!-- truncate -->

## What's new

- **`--engine-name` and `--engine-namespace`** for `registry create` — pin a registry to a specific engine release/namespace instead of guessing.
