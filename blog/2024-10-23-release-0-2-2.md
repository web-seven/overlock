---
slug: release-0-2-2
title: "0.2.2: Configuration Knows Where to Land"
authors: [overlock]
tags: [release, registry, configuration]
---

`configuration load` now sets the default registry automatically.

<!-- truncate -->

## What's new

- **`configuration load` sets the default registry** — no need to pass `--registry` for the common case.
